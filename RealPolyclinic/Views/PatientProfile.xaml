<UserControl x:Class="RealPolyclinic.Views.PatientProfile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RealPolyclinic.Views"
             xmlns:vm="clr-namespace:RealPolyclinic.ViewModels"
             xmlns:views="clr-namespace:RealPolyclinic.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate x:Name="MedCardTemaplate" DataType="{x:Type vm:MedCardVM}">
            <views:MedCard />
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <Button Content="Go back" Command="{Binding DataContext.PreviousPage,RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=Window}}" ></Button>
        </StackPanel>
        <StackPanel Margin="0 30 0 0">
            <Label>Имя:</Label>
            <TextBlock Text="{Binding InfoPat.FirstName}"/>
            <Label>Фамилия:</Label>
            <TextBlock Text="{Binding InfoPat.SurName}"/>
            <Label>Отчество:</Label>
            <TextBlock Text="{Binding InfoPat.Patronymic}"/>
            <WrapPanel Margin="0 0 0 10" Orientation="Vertical">
                <StackPanel Orientation="Horizontal">
                    <Label>Наличие Мед.Карты:</Label>
                    <TextBlock Text="{Binding Check_Card}"/>
                </StackPanel>
                <Button Visibility="{Binding isVisible}" Content="Create_Med_Card" Command="{Binding CreateCard}"></Button>
            </WrapPanel>
            <Button Content="EditProfile" Command="{Binding EdProfile}" CommandParameter="{Binding InfoPat}"/>
            <Button Content="DeleteProfile"/>
            <Button Content="Refresh" Command="{Binding DataContext.GoToPat,RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=Window}}" CommandParameter="{Binding InfoPat.Id}"/>
            <StackPanel  Margin="0 15 0 0">
                <TabControl>
                    <TabItem Header="Personal Info">
                        <Label>In Process</Label>
                    </TabItem>
                    <TabItem Header="Med Card">
                        <ContentControl Visibility="{Binding VisibleCard}" Content="{Binding ShowMedCard}"></ContentControl>
                    </TabItem>
                    <TabItem Header="Directions To Recieve">
                        future records
                    </TabItem>
                </TabControl>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
